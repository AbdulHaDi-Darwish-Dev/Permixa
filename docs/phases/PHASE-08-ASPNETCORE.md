# Phase 8 — ASP.NET Core integration

## Goal

Host-facing JWT bearer authentication, permission authorization handler, exception/problem-details mapping, result helpers.

## Implemented

`Permixa.AspNetCore` library. **No** production controllers (hosts own endpoints). Test host endpoints in AspNetCore.Tests only.

## Follow-up

Phase 9 integration scenarios.
